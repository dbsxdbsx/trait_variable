//! This module is used to test if everything is okay
//! when the trait and struct using the crate macros are defined in different files.

// #[test]
// fn test() {
//     let s = MyStruct { x: 1 };
//     assert_eq!(s.get_value(), 6);
// }
